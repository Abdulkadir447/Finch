/**
 * Co-op Design System — token barrel.
 *
 * Everything a component may need for styling, in one import:
 *
 *   import { colors, type, spacing, radius, shadow, motion, aiGradient }
 *     from '../../theme';
 */
export { colors, aiGradient, aiGradientBorder, tint } from './colors';
export type { ColorToken } from './colors';
export { fontFamily, fontWeight, type } from './typography';
export type { TypeStyle, TextStyleName } from './typography';
export { motion, transition } from './motion';
export { spacing, radius, shadow, z, layout } from './tokens';
export type { SpacingToken } from './tokens';
